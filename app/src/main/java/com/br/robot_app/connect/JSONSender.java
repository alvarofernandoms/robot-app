package com.br.robot_app.connect;

import java.io.File;

/**
 * Responsiable for creating and send a JSON file to the server
 */
public class JSONSender {

    private String filePath = null; // TODO: function to locate the path

    /**
     * Send JSON file to the server
     *
     * @param jsonFile assume that the file is in a correct JSON file construction
     * @return success statemant of the execution
     */
    public boolean sendJSON(File jsonFile){
        boolean success = false;
        return success;
    }

}
